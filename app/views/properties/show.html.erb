<h2><%= @property.name %></h2>
<p><%= @property.property_type %></p>
<p><%= @property.description %></p>


<h3>Add a Task</h3>
<%# render the partial to add a task %>
<%= render 'tasks/taskform', property: @property, task: @task %>

<%# TODO: render the partial to view the tasks. %>
